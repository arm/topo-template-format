{
  "name": "topo-template-format",
  "version": "1.0.0",
  "description": "Topo Template Format documentation",
  "scripts": {
    "format": "prettier --write .",
    "check": "prettier --check ."
  },
  "devDependencies": {
    "prettier": "^3.8.1"
  }
}
