{
  "name": "topo-template-format",
  "version": "1.0.0",
  "description": "Topo Template Format documentation",
  "scripts": {
    "format": "prettier --write .",
    "sync:skills": "node scripts/sync-skill-context.js",
    "check:skills": "node scripts/sync-skill-context.js --check",
    "check": "prettier --check . && npm run check:skills"
  },
  "devDependencies": {
    "prettier": "^3.8.1"
  }
}
